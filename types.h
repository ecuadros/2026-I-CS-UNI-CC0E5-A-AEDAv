#ifndef __TYPES_H__
#define __TYPES_H__

#include <cstddef>

using namespace std;

using Type = int;
using T1 = int;
using Ref = long;

using TypeBTree = char;
using Size = size_t;
using SIndex = ptrdiff_t;
using Level = size_t;
using Flag = bool;
using Byte = unsigned char;
using Token = char;

#endif // __TYPES_H__
